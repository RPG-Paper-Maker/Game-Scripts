import { Base } from "./Base.js";
/**
 * An event class who control the screen fading.
 *
 * @class FadeScreen
 * @extends {Base}
 */
class FadeScreen extends Base {
    constructor(command) {
        super();
        let iterator = {
            i: 0
        };
    }
}
